<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Topological Sorting</title>
    <url>/2024/08/30/Topological%20Sorting/</url>
    <content><![CDATA[<h3 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h3><p>In computer science, a topological sort or topological ordering of a <strong>directed graph</strong> is a linear ordering of its vertices. For instance, the vertices of the graph may represent tasks to be performed, and the edges may represent constraints that one task ust be performed before another; in this application, a topological ordering is just a valid sequence for the tasks.</p>
<p><strong>A topological ordering is possible if and only if the grapg has no directed cycles</strong>,that is, if it is a directed acyclic graph (DAG).</p>
<h3 id="Example"><a href="#Example" class="headerlink" title="Example"></a>Example</h3><p><img src="https://hackmd.io/_uploads/HJTxqmVjR.png" alt="image"></p>
<p>This graph has many valid topological sorts, including:</p>
<ul>
<li>5,7,3,11,8,2,9,10</li>
<li>3,5,7,8,11,2,9,10</li>
<li>3,5,7,8,11,2,10,9</li>
<li>5,7,3,8,11,2,10,9</li>
<li>7,5,11,3,10,8,9,2</li>
<li>5,7,11,2,3,8,9,10</li>
<li>3,7,8,5,11,10,2,9</li>
</ul>
<h3 id="Algorithms"><a href="#Algorithms" class="headerlink" title="Algorithms"></a>Algorithms</h3><p><strong>Kahn’s algorithm</strong> </p>
<p>Time complexity <em>O(V+E)</em>, Space <em>O(V)</em></p>
<ol>
<li>convert input data to a directed graph.</li>
<li>use an array to record the incoming edges of  vertices.</li>
<li>add the nodes which has no incoming edge into a queue.</li>
<li>use the queue to traverse the graph.</li>
<li>repeat 3 and 4 tile the queue is empty.</li>
</ol>
<p><strong>DFS</strong> </p>
<p>Time complexity <em>O(V+E)</em>, Space <em>O(V+E)</em></p>
<ol>
<li>convert input data to a directed graph.</li>
<li>use an array to mark vertices’s states, each vertex has three states including initial, temporary and permanent. If we visite a temporary vertex then return false(cycle detect ), if we visite a permanent vertex, then return true. (to avoid repeated traversal) </li>
<li>add a permanent vertex into the top of a stack.</li>
<li>repeat 3 and 4 tile all vertices was visited.</li>
</ol>
<h3 id="Problems"><a href="#Problems" class="headerlink" title="Problems"></a>Problems</h3><p><a href="https://leetcode.com/problems/course-schedule/description/?envType=study-plan-v2&envId=top-interview-150">Course Schedule</a></p>
<pre><code class="cpp">//Kahn&#39;s algorithm
class Solution &#123;
public:
    bool canFinish(int numCourses, vector&lt;vector&lt;int&gt;&gt;&amp; prerequisites) &#123;
        vector&lt;int&gt;adj[numCourses];
        vector&lt;int&gt;indegree(numCourses,0);
        int ans=0;
        for(auto x:prerequisites)&#123;
            adj[x[1]].push_back(x[0]); //construct a graph (step 1)
            indegree[x[0]]++; //step2
        &#125;
        queue&lt;int&gt;q;
        for(int i=0;i&lt;numCourses;i++)&#123;
            if(indegree[i]==0) q.push(i); //step 3
        &#125;
        while(!q.empty())&#123;
            int now=q.front();
            ans++;
            q.pop();
            for(auto x:adj[now])&#123;
                indegree[x]--;
                if(indegree[x]==0) q.push(x);
            &#125;
        &#125;
        return ans==numCourses;
    &#125;
&#125;;
</code></pre>
<pre><code class="cpp">//DFS
class Solution &#123;
public:
    bool canFinish(int numCourses, vector&lt;vector&lt;int&gt;&gt;&amp; prerequisites) &#123;
        vector&lt;vector&lt;int&gt;&gt;adj(numCourses);
        vector&lt;int&gt;mark(numCourses,0);
        bool rst;
        int ans=0;
        for(auto x:prerequisites)&#123;
            adj[x[1]].push_back(x[0]);
        &#125;
        //mark[i]=0 initial
        //mark[i]=1 temporary
        //mark[i]=2 permanent
        for(int i=0;i&lt;numCourses;i++)&#123;
            if(mark[i]&lt;2)&#123;
                rst=dfs(i,mark,adj);
                if(!rst) return false;
            &#125;
        &#125;
        return true;
    &#125;
    bool dfs(int i,vector&lt;int&gt;&amp;mark,vector&lt;vector&lt;int&gt;&gt;&amp;adj)&#123;
        if(mark[i]==2) return true;
        if(mark[i]==1) return false; //detect cycle
        mark[i]=1;
        for(auto x:adj[i])&#123;
            if(!dfs(x,mark,adj)) return false;
        &#125;
        mark[i]=2;
        return true;
    &#125;
&#125;;
</code></pre>
<p><a href="https://leetcode.com/problems/course-schedule-ii/description/?envType=study-plan-v2&envId=top-interview-150">Course Schedule II</a></p>
<pre><code class="cpp">//Kahn&#39;s algorithm
class Solution &#123;
public:
    vector&lt;int&gt; findOrder(int numCourses, vector&lt;vector&lt;int&gt;&gt;&amp; prerequisites) &#123;
        vector&lt;int&gt;adj[numCourses];
        vector&lt;int&gt;indegree(numCourses,0);
        vector&lt;int&gt;ans;
        for(auto x:prerequisites)&#123;
            adj[x[1]].push_back(x[0]);
            indegree[x[0]]++;
        &#125;
        queue&lt;int&gt;q;
        for(int i=0;i&lt;numCourses;i++)&#123;
            if(indegree[i]==0) q.push(i);
        &#125;
        while(!q.empty())&#123;
            int now=q.front();
            ans.push_back(now);
            q.pop();
            for(auto x:adj[now])&#123;
                indegree[x]--;
                if(indegree[x]==0) q.push(x);
            &#125;
        &#125;
        if(ans.size()!=numCourses)&#123;
            return &#123;&#125;;
        &#125;
        return ans;
    &#125;
&#125;;
</code></pre>
<p>&#96;<br><a href="https://leetcode.com/problems/build-a-matrix-with-conditions/description/">Build a Matrix with Conditions</a></p>
<pre><code class="cpp">class Solution &#123;
public:
    vector&lt;vector&lt;int&gt;&gt; buildMatrix(int k, vector&lt;vector&lt;int&gt;&gt;&amp; rowConditions, vector&lt;vector&lt;int&gt;&gt;&amp; colConditions) &#123;
        vector&lt;vector&lt;int&gt;&gt;rowgraph(k+1),colgraph(k+1);
        for(auto&amp; rc:rowConditions)&#123;
            rowgraph[rc[0]].push_back(rc[1]);
        &#125;
        for(auto&amp; cc:colConditions)&#123;
            colgraph[cc[0]].push_back(cc[1]);
        &#125;
        vector&lt;int&gt; roworder=topoSort(rowgraph,k);
        vector&lt;int&gt; colorder=topoSort(colgraph,k);

        if(roworder.empty()||colorder.empty()) return&#123;&#125;;
        vector&lt;vector&lt;int&gt;&gt;result(k,vector&lt;int&gt;(k,0));
        unordered_map&lt;int,int&gt;rowMap,colMap;
        for(int i=0;i&lt;k;++i)&#123;
        // i represents position, order[i] represents number
            rowMap[roworder[i]]=i;
            colMap[colorder[i]]=i;
        &#125;
        for(int i=1;i&lt;=k;i++)&#123;
        //Map[i] represent i&#39;s position
            result[rowMap[i]][colMap[i]]=i;
        &#125;
        return result;
    &#125;
    
    //Kahn&#39;s algorithm
    vector&lt;int&gt; topoSort(vector&lt;vector&lt;int&gt;&gt; &amp;graph, int k)&#123;
        vector&lt;int&gt;indegree(k+1,0),order;
        queue&lt;int&gt;q;
        for(int i=1;i&lt;=k;i++)&#123;
            for(int j:graph[i])&#123;
                ++indegree[j];
            &#125;
        &#125;
        for(int i=1;i&lt;=k;i++)&#123;
            if(indegree[i]==0) q.push(i);
        &#125;
        while(!q.empty())&#123;
            int node=q.front();
            q.pop();
            order.push_back(node);
            for(int adj:graph[node])&#123;
                if(--indegree[adj]==0) q.push(adj);
            &#125;
        &#125;
        return order.size()==k? order:vector&lt;int&gt;();
    &#125;
&#125;;
</code></pre>
]]></content>
      <categories>
        <category>gallery</category>
        <category>Algorithm</category>
        <category>Graph</category>
      </categories>
      <tags>
        <tag>Algorithm</tag>
        <tag>Graph</tag>
      </tags>
  </entry>
  <entry>
    <title>Binary Tree DFS</title>
    <url>/2024/09/06/Binary%20Tree%20DFS/</url>
    <content><![CDATA[<h3 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h3><p>In computer science, tree traversal is a form of graph travrsal and refers to the process of <strong>visiting each node in a tree data structure, exactly once</strong>. Such traversals are classified by the order in which the nodes are visited.</p>
<h3 id="DFS"><a href="#DFS" class="headerlink" title="DFS"></a>DFS</h3><p>In depth-first search(DFS), the search tree is deepened as much as possible before going to the next sibling. To traverse binary trees with depth-first search, perform the following operations at each node:</p>
<ol>
<li>If the current node is empty then return</li>
<li>Execute the following tree operations in a certain order<br> N: Visit the current node.<br> L: <strong>Recursively</strong> traverse the current node’s left subtree<br> R: <strong>Recursively</strong> traverse the current node’s right subtree.</li>
</ol>
<p><strong>Pre-order, NLR</strong><br><strong>Post-order, LRN</strong><br><strong>In-order, LNR</strong><br><strong>Reverse pre-order, NRL</strong><br><strong>Reverse post-order, RLN</strong><br><strong>Reverse  in-order, RNL</strong></p>
<p><strong>Given a tree with distinct elements, either pre-order or post-order paired with in-order is sufficient to describe the tree uniquely.</strong> However, pre-order with post-order leaves some ambiguity in the tree structure. </p>
<h3 id="Problems"><a href="#Problems" class="headerlink" title="Problems"></a>Problems</h3><p><a href="https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal">Construct Binary Tree from Preorder and Inorder Traversal</a></p>
<pre><code class=".cpp">class Solution &#123;
public:
    int pidx;
    unordered_map&lt;int,int&gt; mp;
    TreeNode* buildTree(vector&lt;int&gt;&amp; preorder, vector&lt;int&gt;&amp; inorder) &#123;
        pidx=0;
        int len=inorder.size();
        for(int i=0;i&lt;len;i++)&#123;
            mp[inorder[i]]=i;
        &#125;
        return construct(0,len-1,preorder);
    &#125;
    TreeNode* construct(int start,int end,vector&lt;int&gt;&amp;preorder)&#123;
        if(start&gt;end) return NULL;
        int rootval=preorder[pidx];
        TreeNode *head= new TreeNode(rootval);
        int mid=mp[rootval];
        pidx++;
        head-&gt;left=construct(start,mid-1,preorder);
        head-&gt;right=construct(mid+1,end,preorder);
        return head;
    &#125;
&#125;;
</code></pre>
<p><a href="https://leetcode.com/problems/construct-binary-tree-from-inorder-and-postorder-traversal">Construct Binary Tree from inorder and postorder traversal</a></p>
<pre><code class="cpp">class Solution &#123;
public:
    int pidx;
    unordered_map&lt;int,int&gt; mp;
    TreeNode* buildTree(vector&lt;int&gt;&amp; inorder, vector&lt;int&gt;&amp; postorder) &#123;
        int len=inorder.size();
        pidx=len-1;
        for(int i=0;i&lt;len;i++)&#123;
            mp[inorder[i]]=i;
        &#125;
        return construct(0,len-1,postorder);
    &#125;
    TreeNode* construct(int start,int end,vector&lt;int&gt;&amp;postorder)&#123;
        if(start&gt;end) return NULL;
        int rootval=postorder[pidx];
        TreeNode* head= new TreeNode(rootval);
        int mid=mp[rootval];
        pidx--;
        head-&gt;right=construct(mid+1,end,postorder);
        head-&gt;left=construct(start,mid-1,postorder);
        return head;
    &#125;
&#125;;
</code></pre>
<p>Everytime we create a new node with a number from preorder array. That should be root node of each subtree, because in preorder, we traverse the tree like root, left and right. So let’s say middle(&#x3D;root)<br>To create left and right side of tree, we use two pointers, let’s say start and end.<br>Start and end specify the range of the current subtree in the inorder list.<br>Within this range, the root node is determined, and itd index is used to recursively set the ranges for the left and right subtrees.</p>
<p><a href="https://leetcode.com/problems/binary-tree-maximum-path-sum/">Binary Tree Maximum Path Sum</a></p>
<pre><code class="cpp">class Solution &#123;
public:
    int max_sum=INT_MIN;
    int maxPathSum(TreeNode* root) &#123;
        max_gain(root);
        return max_sum;
    &#125;
    int max_gain(TreeNode* root)&#123;
        if(!root) return 0;
        int l=max(max_gain(root-&gt;left),0);
        int r=max(max_gain(root-&gt;right),0);
        int new_price=root-&gt;val+l+r;
        max_sum=max(max_sum,new_price);
        return root-&gt;val+max(l,r);
    &#125;
&#125;;
</code></pre>
<p>In this problem we use <strong>postorder traversal</strong> to calculate the maximum path sum of subtrees. <strong>For each node we can choose either its left subtree or right subtree</strong>, so our return value is <code>root-&gt;val+max(l,r)</code>  However, each node may be an end point, so the correct answer of maximum sum is <code>int new_price=root-&gt;val+l+r; max_sum=max(max_sum,new_price);</code></p>
<p><a href="https://leetcode.com/problems/flatten-binary-tree-to-linked-list/">Flatten Binary Tree to Linked List</a></p>
<pre><code class="cpp">class Solution &#123;
public:
    void flatten(TreeNode* root) &#123;
        if(root) preorder(root); 
    &#125;
    TreeNode* preorder(TreeNode* root)&#123;
        TreeNode* leftmost=root;
        if(root)&#123;
            TreeNode* left=root-&gt;left;
            TreeNode* right=root-&gt;right;
            root-&gt;right=left;
            root-&gt;left=NULL;
            if(left) leftmost=preorder(left); //deal with its left subtree
            if(right)&#123;
               leftmost-&gt;right=right;
               leftmost=preorder(right); //deal with its right subtree
            &#125;
        &#125;
        return leftmost;
    &#125;
&#125;;
</code></pre>
<p>If we notice carefully in the flattened tree, each node’s right child points to the next node of a <strong>pre-order traversal</strong>, so we return the leftmost node untile all left node was added to the list and we started to deal with right node( though the variable name still called leftmost )</p>
<h3 id="Common-Data-Structure-in-DFS"><a href="#Common-Data-Structure-in-DFS" class="headerlink" title="Common Data Structure in DFS"></a>Common Data Structure in DFS</h3><p>I prefer use recursive function to implement DFS, however stack is also an useful data structure to implement DFS. There is a problem ask programers useing stack to implement Binary Search Tree Inorder Iterator.<br><a href="https://leetcode.com/problems/binary-search-tree-iterator">Binary Search Tree Iterator</a></p>
<pre><code class="cpp">//Inorder
class BSTIterator &#123;
public:
    stack&lt;TreeNode*&gt;s;
    BSTIterator(TreeNode* root) &#123;
        Inorder(root);
    &#125;
    
    int next() &#123;
        TreeNode* top=s.top();
        s.pop();
        Inorder(top-&gt;right);
        return top-&gt;val;
    &#125;
    
    bool hasNext() &#123;
        return !s.empty();
    &#125;
    void Inorder(TreeNode* root)&#123;
        while(root)&#123;
            s.push(root);
            root=root-&gt;left;
        &#125;
    &#125;
&#125;;
</code></pre>
<p>Notice that we should put a node’s left child on the top of the stack to ensure the ordering is in-order. If we want to change it to pre-order or post-order we can modify our code to this</p>
<pre><code class="cpp">//Preorder
class BSTIterator &#123;
public:
    stack&lt;TreeNode*&gt;s;
    TreeNode *now;
    BSTIterator(TreeNode* root) &#123;
        s.push(root);
    &#125;
    
    int next() &#123;
        TreeNode *now=s.top();
        Preorder(now);
        s.pop();
        return now-&gt;val;
    &#125;
    
    bool hasNext() &#123;
        return !s.empty();
    &#125;
    void Preorder(TreeNode* root)&#123;
        if(root-&gt;right) s.push(root-&gt;right);
        if(root-&gt;left) s.push(root-&gt;left);
    &#125;
&#125;;
</code></pre>
<pre><code class="cpp">//Postorder
class BSTIterator &#123;
public:
    stack&lt;TreeNode*&gt;s;
    BSTIterator(TreeNode* root) &#123;
        Postorder(root);
    &#125;
    
    int next() &#123;
        TreeNode *top=s.top();
        s.pop();
        Postorder(top-&gt;left);
        return top&gt;val;
    &#125;
    
    bool hasNext() &#123;
        return !s.empty();
    &#125;
    void Postorder(TreeNode* root)&#123;
        while(root)&#123;
            s.push(root);
            if(root-&gt;right) s.push(root-&gt;right);
            root=root-&gt;left;
        &#125;
    &#125;
&#125;;
</code></pre>
<h3 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h3><p>The time and space analysis of DFS differs according to its application area.In theoretical computer science, DFS is typically used to traverse an entire graph(tree), and takes time $O(|V|+|E|)$. This is linear in the size of the graph. In these applications it also uses space $O(|V|)$  in the worst case to store the stack of vertices on the current search path as well as the set of already-visited vertices. Thus, in this setting, the time and bounds are the same as fpr breadth-first search and the choice of which of these two algorithms to use depends less on their complexity and more on the different properties of the vertex orderings the two algorithms produce.</p>
]]></content>
      <categories>
        <category>gallery</category>
        <category>Algorithm</category>
        <category>Binary Tree</category>
      </categories>
      <tags>
        <tag>Algorithm</tag>
        <tag>Binary Tree</tag>
      </tags>
  </entry>
  <entry>
    <title></title>
    <url>/about/index.html</url>
    <content><![CDATA[<p>Live in Taiwan, write something about computer science.</p>
]]></content>
  </entry>
</search>
